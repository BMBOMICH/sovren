# pt6 - staged index: "(i + 1) * 8" builds rsi first, then the
# pure term evaluates, then add [rdi+rsi*8], rax. v[2] 60 + 4*9.
use sys

v: 0

v: _sov_alloc(640)
poke_i64(v, 16, 60)
i: 1
a: 4
b: 9
poke_i64(v, (i + 1) * 8, peek_i64(v, (i + 1) * 8) + a * b)
print peek_i64(v, 16)
