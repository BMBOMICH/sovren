# xe1 - the everyday except name: a plain function-local out of
# thin air. untouched by pass 68; binary byte-identical.
use sys

main:
    try
        raise "boom"
    except e
        print 41
    print 42
