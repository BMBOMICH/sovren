# gl4 - twin select with a global base (pk1's twin pick was
# register-based): the compare loads ride the direct global load.
use sys

a: 0

main:
    a: _sov_alloc(8000)
    r: 2
    poke_i64(a, r * 8, 510)
    poke_i64(a, (r + 1) * 8, 777)
    if peek_i64(a, r * 8) < peek_i64(a, (r + 1) * 8)
        r: r + 1
    print r
    print peek_i64(a, r * 8)
