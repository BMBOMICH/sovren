# dp4 - decline: bases differ, the arm must not fire. binary
# byte-identical under both compilers.
use sys

g: 0

h: 0

g: _sov_alloc(640)
h: _sov_alloc(640)
poke_i64(g, 8, 23)
poke_i64(h, 8, 61)
i: 1
d: peek_i64(g, i * 8) - peek_i64(h, i * 8)
print d
