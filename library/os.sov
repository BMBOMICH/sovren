# os.sov — process && machine
use sys

os_pid:
    return sys_getpid()

os_exit code:
    sys_exit(code)
    return code

os_cwd:
    buf: _sov_alloc(4096)
    if sys_getcwd(buf, 4095) is 0
        return blank
    return buf

os_cd path:
    if sys_chdir(path) is 0
        return 1
    return 0
