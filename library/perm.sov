# perm.sov — file mode
use sys

perm_set path mode:
    if syscall(90, path, mode, 0, 0, 0, 0) is 0
        return 1
    return 0

perm_exec path:
    return perm_set(path, 493)

perm_priv path:
    return perm_set(path, 384)
