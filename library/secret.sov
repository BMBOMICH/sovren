# secret.sov — hide a word on screen
use string

secret_mask s:
    n: str_len(s)
    out: _sov_alloc(n + 1)
    i: 0
    as long as i < n
        poke_byte(out, i, 42)
        i: i + 1
    poke_byte(out, n, 0)
    return as text out

secret_wipe s:
    wipe s
    return 1
