# fuzz_run.sov — run the Sovren-written fuzzer
use fuzzlib
main:
    fz_seed: 987654321
    bad: fz_run(100)
    if bad is 0
        print "fuzz ok"
    if not
        print "fuzz FAILED"
        print bad
